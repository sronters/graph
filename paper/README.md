# GraphTrust NSRI submission

Build the evidence artifacts first, copy the generated `results.tex` and selected figures into this directory, then run:

```bash
make -C paper
```

The main research brief ends before the forced page break preceding references. References, the AI Use Transparency Statement, and the reproducibility appendix are excluded from the NSRI 2–5 page brief limit.
