# Evidence manifest

`evidence_manifest.csv` is generated by `scripts/build_final_results.py` from
verified immutable run directories. The checked-in file is the available
smoke-slice provenance export; a complete final submission must regenerate it
against all 225 confirmed runs and must not replace missing rows with
synthetic identifiers.
